# Additive inference-only overlay for the cached trellis2-nodes environment.
# Deliberately excludes torch, torchvision, CUDA wheels, NumPy, Transformers,
# Lightning, DeepSpeed, FlashAttention, and SageAttention pins.
accelerate>=1.1,<2
diffusers>=0.30,<1
huggingface_hub[hf_xet]>=0.36.0,<2
omegaconf==2.3.0
scikit-image>=0.24,<1
